#/bin/sh

# Desc:
# Start docker manually to generate initial files

systemctl stop docker.service

/usr/bin/dockerd -D -H tcp://0.0.0.0 &
DOCKER_PID=$?
sleep 5
kill $DOCKER_PID

systemctl start docker.service

